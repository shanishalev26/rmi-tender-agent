# סוכנת AI לאיתור וניתוח ראשוני של מכרזי רמ״י

אב־טיפוס לסוכנת שמושכת מכרזים פעילים מרשות מקרקעי ישראל (רמ״י), מורידה את חוברת המכרז, מחלצת ממנה מידע מובנה באמצעות מודל AI, מאתרת את התב״ע הרלוונטית במאגר התכנון, ומדרגת כל מכרז מול פרופיל של חברת נדל״ן — עם הסבר לכל ציון והפרדה ברורה בין מידע שנמשך מהמקור לבין מסקנות המערכת.

---

## תוכן עניינים

- [הוראות הפעלה](#הוראות-הפעלה)
- [תיאור הארכיטקטורה](#תיאור-הארכיטקטורה)
- [מקורות המידע ואופן החיבור](#מקורות-המידע-ואופן-החיבור)
- [אופן השימוש במודל ה-AI](#אופן-השימוש-במודל-ה-ai)
- [מנגנון ההתאמה](#מנגנון-ההתאמה)
- [מבנה הנתונים](#מבנה-הנתונים)
- [מגבלות שזוהו](#מגבלות-שזוהו)
- [הצעות להמשך](#הצעות-להמשך)
---

## הדגמת המערכת

הדגמה מלאה של המערכת על נתונים אמיתיים — חמישה מכרזים שנמשכו אוטומטית, מכרז שעבר ניתוח מלא, תב״ע שאותרה, והתאמה לפרופיל חברה — יחד עם האתגרים המרכזיים שעלו בפיתוח, נמצאת במסמך [DEMO.md](DEMO.md).

---

## הוראות הפעלה

### דרישות מקדימות

- Python 3.10 ומעלה
- מפתח API של Anthropic

### התקנה

```bash
git clone https://github.com/shanishalev26/rmi-tender-agent.git
cd rmi-tender-agent

python -m venv venv
source venv/bin/activate        # ב-Windows: venv\Scripts\activate

pip install -r requirements.txt
```

### הגדרת מפתח ה-API

המפתח נשמר במשתנה סביבה ולא בקוד. יש ליצור קובץ `.env` בשורש הפרויקט:

```
ANTHROPIC_API_KEY=sk-ant-...
```

בדיקה שהחיבור למודל עובד:

```bash
python test_ai.py
```

### הרצת הצינור המלא

הסדר הבא בונה את מאגר הנתונים מאפס ומריץ את כל שלבי העיבוד:

```bash
# 1. משיכת רשימת המכרזים הפעילים מרמ״י
python fetch_tenders.py

# 2. בניית מאגר הניתוח: לכל מכרז — פרטים, חוברת, חילוץ AI,
#    חיפוש תב״ע וחישוב התאמה לפרופיל
python build_dataset.py --count 7

# 3. הפעלת הממשק
streamlit run app.py
```

`build_dataset.py` בוחר אוטומטית את המכרזים שמועד הסגירה שלהם הקרוב ביותר ועדיין בעתיד, ומדלג על מכרזים שכבר נותחו. כדי לנתח מחדש מכרזים קיימים:

```bash
python build_dataset.py --count 7 --refresh
```

### הרצת שלב בודד (לצורכי פיתוח ובדיקה)

כל מודול ניתן להרצה עצמאית עבור מכרז יחיד:

```bash
python analyze.py 20260639     # פרטי המכרז + הורדת חוברת + חילוץ AI
python planning.py 20260639    # חיפוש התב״ע במאגר התכנון
python matching.py             # חישוב מחדש של ההתאמה לכל המכרזים
```

---

## תיאור הארכיטקטורה

המערכת בנויה כצינור עיבוד (pipeline) שבו כל שלב אחראי על תחום אחד בלבד, ומעביר את התוצאה הלאה דרך קובץ JSON מרכזי לכל מכרז. ההפרדה הזו — בין החיבור למאגרים, חילוץ המסמכים ולוגיקת ההתאמה — מאפשרת להריץ, לבדוק ולהחליף כל שלב בנפרד.

```
fetch_tenders.py        משיכת רשימת המכרזים הפעילים          → data/tenders.json
        │
build_dataset.py        בחירת המכרזים הקרובים לסגירה
        │                והרצת הצינור על כל אחד
        ├──► analyze.py         פרטי המכרז + הורדת חוברת + חילוץ AI
        │        ├── get_booklet.py   חיבור ל-API של רמ״י, חילוץ עובדות, הורדת PDF
        │        └── extract.py       חילוץ שדות מהחוברת באמצעות Claude
        │
        ├──► planning.py        חיפוש התב״ע במאגר התכנון ובחירת התכנית
        │
        └──► matching.py        חישוב ציון ההתאמה לפרופיל החברה
                     │
        company_profile.py      טעינה, אימות ושמירה של פרופיל החברה
                     │
                app.py          ממשק Streamlit להצגת הכול
```

### תפקידי הקבצים

<table dir="rtl">
  <thead>
    <tr><th>קובץ</th><th>תפקיד</th></tr>
  </thead>
  <tbody>
    <tr><td><code>fetch_tenders.py</code></td><td>מושך את רשימת המכרזים הפעילים מ-API החיפוש של רמ״י ושומר ל-<code>data/tenders.json</code>.</td></tr>
    <tr><td><code>build_dataset.py</code></td><td>מתזמר (orchestrator). בוחר את המכרזים הקרובים לסגירה ומריץ עליהם את <code>analyze → planning → matching</code>. מדלג על מכרזים שכבר עובדו כדי למנוע כפילויות.</td></tr>
    <tr><td><code>analyze.py</code></td><td>מריץ את כל שלב העיבוד עבור מכרז יחיד ומאחד את התוצאות לרשומת JSON אחת עם הפרדת מקורות. כולל גם <code>refresh_rmi_data</code> לרענון נתוני רמ״י בלבד.</td></tr>
    <tr><td><code>get_booklet.py</code></td><td>כל התקשורת מול ה-API של רמ״י: פרטי המכרז, טבלאות קוד (סוג מכרז, יישוב, ייעוד), חילוץ העובדות המובנות והורדת חוברת המכרז.</td></tr>
    <tr><td><code>extract.py</code></td><td>שולח את חוברת ה-PDF ל-Claude ומחלץ שדות מובנים: מספר תכנית, תנאי סף, ונקודה לבדיקה — כל אחד עם מקור, עמוד, ציטוט ורמת ביטחון.</td></tr>
    <tr><td><code>planning.py</code></td><td>מחפש את התב״ע במאגר התכנון של רמ״י לפי מספר תכנית וגוש/חלקה, מדרג את המועמדות ובוחר תכנית עם הסבר.</td></tr>
    <tr><td><code>matching.py</code></td><td>מחשב ציון התאמה 0–100 מול פרופיל החברה, לפי כללים ומשקלים מפורשים, עם הסבר לכל קריטריון.</td></tr>
    <tr><td><code>company_profile.py</code></td><td>טעינה, אימות ושמירה של פרופיל החברה. יוצר פרופיל ברירת מחדל אם עדיין אין.</td></tr>
    <tr><td><code>app.py</code></td><td>ממשק Streamlit: רשימת מכרזים, עמוד מכרז מפורט, והגדרות חברה.</td></tr>
    <tr><td><code>test_ai.py</code></td><td>בדיקת עשן קצרה לחיבור מול Anthropic.</td></tr>
  </tbody>
</table>

### עקרון מנחה: הפרדת מקורות

לכל מכרז נשמרת רשומה אחת תחת `data/analysis/<michraz_id>.json`, שבה כל מקור מידע יושב בשדה נפרד תחת `sources` — `rmi_api`, `tender_booklet_ai`, `planning_api` — ולכל אחד סטטוס משלו. המסקנות של המערכת (ציון ההתאמה) נשמרות בנפרד תחת `derived_analysis`. כך תמיד ברור מאיפה הגיע כל נתון, והאם הוא עובדה מהמקור או מסקנה של המערכת.

---

## מקורות המידע ואופן החיבור

המערכת מתחברת לשלושה מקורות ציבוריים, כולם של רמ״י, וכולם דרך אותו דפוס: פתיחת session, בקשת "חימום" לעמוד הראשי שאוספת cookies, ואז קריאות ל-API הפנימי שהאתר עצמו משתמש בו.

### 1. מאגר מכרזי רמ״י

- **עמוד המקור:** `apps.land.gov.il/MichrazimSite`
- **רשימת המכרזים:** `POST /api/SearchApi/Search` — מוחזרים המכרזים הפעילים.
- **פרטי מכרז:** `GET /api/MichrazDetailsApi/Get` — ה-JSON הגולמי המלא של מכרז בודד, שנשמר במלואו.
- **הורדת מסמכים:** `POST /api/MichrazDetailsApi/GetFileContent` — הורדת חוברת המכרז.
- **טבלאות קוד:** `GeneralTablesApi` ו-`YeshuvimApi` — לתרגום קודים מספריים לשמות קריאים (סוג מכרז, יישוב, ייעוד).

חוברת המכרז מזוהה לפי המסמך שתיאורו "חוברת המכרז" ברשימת המסמכים. כאשר לא נמצאת חוברת, הדבר מסומן מפורשות (`processing_status: not_found`) והצינור ממשיך בלעדיה.

### 2. מאגר התכניות (תב״ע)

- **עמוד המקור:** `apps.land.gov.il/TabaSearch`
- **חיפוש תכניות:** `POST /api/SerachPlans/GetPlans` — לפי מספר תכנית או גוש/חלקה.
- **פרטי תכנית:** `GET /api/Plan/GetPlanData` — לשליפת הייעוד המרכזי, יחידות הדיור ומסמכי התכנית עבור התכנית שנבחרה.

### 3. מודל ה-AI

- **ספק:** Anthropic (Claude), דרך ה-SDK הרשמי `anthropic`.
- **חיבור:** מפתח ה-API נטען מ-`.env` דרך `python-dotenv`, ולעולם אינו נשמר בקוד.

---

## אופן השימוש במודל ה-AI

מודל ה-AI משמש למשימה אחת וממוקדת: חילוץ מידע מובנה מתוך חוברת מכרז — מסמך PDF ארוך בעברית שהמבנה שלו משתנה ממכרז למכרז.

- **המודל:** `claude-sonnet-5`.
- **הקלט:** קובץ ה-PDF של החוברת נשלח ישירות למודל (מקודד ב-base64), יחד עם הנחיה מובנית.
- **הפלט:** JSON בלבד, עם שלושה שדות — `plan_number` (מספר התב״ע), `threshold_conditions` (תנאי סף), ו-`point_to_check` (נקודה משמעותית לבדיקה).

עקרונות שנבנו לתוך ההנחיה כדי לשמור על אמינות:

- **בלי המצאות.** המודל מונחה להשתמש אך ורק במידע שמופיע בחוברת, ולא להשלים ממקורות חיצוניים.
- **מקור לכל נתון.** לכל שדה שחולץ המודל מחזיר את מסמך המקור, מספר העמוד, ציטוט מילולי מדויק מהחוברת, ורמת ביטחון (`high` / `medium` / `low`).
- **הבחנה מפורשת בין מושגים.** ההנחיה כוללת כללים למניעת בלבול נפוץ — למשל בין מספר תכנית לבין מספר גוש, חלקה, מגרש או היתר; ובין תוקף ערבות לבין סכום ערבות.
- **סימון מפורש של מידע חסר.** כאשר נתון אינו מופיע בבירור, המודל מחזיר `value: null` עם `confidence: low` במקום לנחש.

הפלט של המודל מיושב מול נתוני ה-API של רמ״י: לפני חיפוש התב״ע, מספר התכנית שחילץ ה-AI נבדק מול הגושים, החלקות והמגרשים הידועים, וכך נמנעת טעות שבה מספר גוש מזוהה בטעות כמספר תכנית.

---

## מנגנון ההתאמה

לכל מכרז מחושב ציון התאמה בין 0 ל-100 מול פרופיל החברה, לפי שישה קריטריונים עם משקלים מפורשים (`matching.py`):

<table dir="rtl">
  <thead>
    <tr><th>קריטריון</th><th>משקל</th></tr>
  </thead>
  <tbody>
    <tr><td>אזור פעילות</td><td>20</td></tr>
    <tr><td>סוג פרויקט</td><td>20</td></tr>
    <tr><td>מספר יחידות דיור</td><td>20</td></tr>
    <tr><td>הוצאות פיתוח</td><td>15</td></tr>
    <tr><td>סטטוס תכנוני (תכנית מאושרת)</td><td>15</td></tr>
    <tr><td>זמן עד להגשה</td><td>10</td></tr>
  </tbody>
</table>

לכל קריטריון אחד משלושה סטטוסים: **התאים** (`matched`), **לא התאים** (`not_matched`), או **דורש בירור** (`needs_clarification`) — כאשר המידע הדרוש חסר.

### חישוב שקוף ונקי מהטיה

הציון אינו סכום נקודות פשוט, אלא **מחושב רק מתוך הקריטריונים שעבורם קיים מידע**. קריטריון שדורש בירור אינו נספר לא כהצלחה ולא ככישלון — הוא פשוט מוסר מהמכנה. במקביל מחושב `data_coverage`: כמה מתוך 100 נקודות המשקל הכולל אכן נבדקו. כך מכרז עם מעט מידע לא מקבל ציון נמוך מטעה, אלא ציון גבוה עם כיסוי נמוך — והמשתמש רואה את שניהם.

### מהציון להמלצה

- כיסוי מידע נמוך מ-70% → **דורש בירור** (ללא קשר לציון)
- ציון 80 ומעלה → **התאמה גבוהה**
- ציון 60–79 → **מתאים לבדיקה**
- ציון 40–59 → **דורש בירור**
- ציון מתחת ל-40 → **התאמה נמוכה**

לכל קריטריון נשמר הסבר מילולי (הערך בפועל, הערך הנדרש, והסיבה לסטטוס), והמשקלים מוגדרים במקום אחד (`WEIGHTS`) כך שניתן לשנותם בקלות. פרופיל החברה נערך דרך טופס בממשק, ושמירתו מפעילה חישוב מחדש של ההתאמה לכל המכרזים.

---

## מבנה הנתונים

כל הנתונים נשמרים תחת `data/`:

```
data/
├── tenders.json              רשימת המכרזים הפעילים מרמ״י
├── company_profile.json      פרופיל החברה
├── details/<id>.json         ה-JSON הגולמי המלא של כל מכרז
├── booklets/<id>.pdf         חוברות המכרז שהורדו
└── analysis/<id>.json        הרשומה המאוחדת לכל מכרז
```

הרשומה המאוחדת (`analysis/<id>.json`) בנויה כך:

```
{
  "michraz_id": ...,
  "analyzed_at": ...,
  "documents": [ ... ],          # מטא-דאטה של המסמכים שהורדו
  "sources": {
    "rmi_api":            { "status": ..., "data": ... },   # עובדות מרמ״י
    "tender_booklet_ai":  { "status": ..., "data": ... },   # חילוץ ה-AI
    "planning_api":       { "status": ..., "data": ... }    # תוצאות התב״ע
  },
  "derived_analysis": {
    "company_match": { ... }     # מסקנת המערכת — ציון ההתאמה
  }
}
```

---

## מגבלות שזוהו

- **תלות ב-API לא רשמי.** החיבור מסתמך על ה-API הפנימי של אתרי רמ״י, שאינו מתועד רשמית ועלול להשתנות ללא התראה.
- **חילוץ AI תלוי בחוברת.** כאשר לא נמצאת חוברת מכרז, או כשמספר התכנית אינו מצוין בה במפורש, החילוץ מוגבל. המערכת מסמנת זאת, אך אינה משלימה את המידע החסר.
- **בחירת תב״ע אוטומטית.** כאשר נמצאות מספר תכניות עם ציון התאמה זהה, המערכת נמנעת מבחירה שרירותית ומסמנת שנדרש אימות ידני, במקום לבחור באקראי.
- **התאמת טקסט בסיסית.** השוואת אזורי פעילות וסוגי פרויקטים מבוססת על נרמול והכלת מחרוזות, ולא על מיפוי סמנטי מלא של שמות יישובים או ייעודים.
- **ללא מקביליות.** המכרזים מעובדים בזה אחר זה. עיבוד היקף גדול יידרש לתזמון ולעבודה במקביל.

---

## הצעות להמשך

- **זיהוי שינויים.** סימון מכרזים חדשים מאז הסריקה האחרונה, ושינויים במועד ההגשה או בגרסת החוברת.
- **חילוץ מעמיק יותר.** שליפת מידע נוסף מתוך תקנון התב״ע, והשוואה בין נתוני החוברת לנתוני התכנית לאיתור פערים.
- **תיקון ידני.** אפשרות לערוך נתון שחולץ באופן שגוי, ולתעד את התיקון.
- **תמיכה במספר פרופילים.** ניהול כמה פרופילי חברה במקביל והשוואת דירוג ביניהם.
- **מפה.** הצגת המכרז והתכנית על מפה לפי הגוש והחלקה.
- **סיכום מנהלים ורשימת שאלות.** יצירה אוטומטית של סיכום קצר לכל מכרז ורשימת שאלות ממוקדות לאדריכל, שמאי או עורך דין.
- **התראות.** התראה על מכרז חדש בעל התאמה גבוהה לפרופיל.
